# Heavy profile (Ceph RGW on top of sds-elastic).
# REQUIRES the sds-elastic module installed with a ready ElasticCluster named
# below in elasticClusterRef.
---
apiVersion: storage.deckhouse.io/v1alpha1
kind: ObjectStore
metadata:
  name: heavy
spec:
  type: Heavy
  elasticClusterRef: main   # <-- name of an existing, Ready ElasticCluster
  redundancy: Standard
  reclaimPolicy: Delete
---
apiVersion: storage.deckhouse.io/v1alpha1
kind: Bucket
metadata:
  name: heavy-bucket
spec:
  objectStoreRef: heavy
  reclaimPolicy: Delete
---
apiVersion: storage.deckhouse.io/v1alpha1
kind: BucketClaimPolicy
metadata:
  name: heavy-bucket-obj-test
spec:
  bucketRef: heavy-bucket
  allowedNamespaces:
    names:
      - obj-test
---
apiVersion: storage.deckhouse.io/v1alpha1
kind: BucketClaim
metadata:
  name: heavy-claim
  namespace: obj-test
spec:
  existingBucketName: heavy-bucket
---
apiVersion: storage.deckhouse.io/v1alpha1
kind: BucketAccess
metadata:
  name: heavy-access
  namespace: obj-test
spec:
  bucketClaimName: heavy-claim
  permission: ReadWrite
---
apiVersion: batch/v1
kind: Job
metadata:
  name: s3-test-heavy
  namespace: obj-test
spec:
  backoffLimit: 10
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: mc
          image: minio/mc:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              mc alias set t "$S3_ENDPOINT" "$AWS_ACCESS_KEY_ID" "$AWS_SECRET_ACCESS_KEY"
              echo "hello from sds-object test at $(date)" | mc pipe "t/$S3_BUCKET/hello.txt"
              echo "--- listing ---"; mc ls "t/$S3_BUCKET"
              echo "--- content ---"; mc cat "t/$S3_BUCKET/hello.txt"
              echo "S3 OK"
          envFrom:
            - secretRef:
                name: heavy-access-s3-credentials
