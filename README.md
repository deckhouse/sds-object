# sds-object

Deckhouse Kubernetes Platform module for managing object storage.

> The module is in the `Experimental` stage. The API, configuration, and custom
> resources may change without notice; do not use it for production workloads.

This repository currently holds the initial module skeleton (build, CI, helm
chart, Go application scaffolding). The domain logic is not yet implemented.
